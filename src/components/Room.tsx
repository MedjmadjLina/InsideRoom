// ============================================
// Room.tsx — apartment shell wrapper
// Delegates to the modular partition system.
// ============================================

"use client";

import PartitionSystem from "./PartitionSystem";

export default function Room() {
  return <PartitionSystem />;
}
